{
  "name": "jeffinators website",
  "version": "1.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && vuepress build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jeffinators/jeffinators.github.io.git"
  },
  "dependencies": {
    "vuepress": "^1.3.1"
  }
}
